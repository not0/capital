---
import BaseLayout from "../layouts/BaseLayout.astro";
const base = import.meta.env.BASE_URL;
---

<BaseLayout title="あのあとにひとつ">
  <div
    style={`
    flex: 1;
    background: url('${base}/assets/top/background.png') no-repeat center center;
    background-size: cover;
    min-height: 100vh;
  `}
  >
  </div>
</BaseLayout>
